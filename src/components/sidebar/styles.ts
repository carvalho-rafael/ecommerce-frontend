import styled from 'styled-components';

export const Aside = styled.aside`
    position: sticky;
    top: 0;
    width: 300px;
    height: 100%;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    float: left;
`
