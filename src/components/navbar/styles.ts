import styled from 'styled-components';

export const Header = styled.header`
    position: relative;
    height: 90px;
    background: #cccccc;
    z-index: 999;
    img {
        height: 100%;
    }
`
